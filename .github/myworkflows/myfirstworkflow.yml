name: My First Workflow
on:
  push:
    branches:
      - shareef

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Say Hello
        run: echo "Hello from myworkflows!"
